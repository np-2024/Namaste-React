import React from "react";

const NotFound = ({ text }) => {
  return (
    <div className="flex items-center justify-center flex-1">
      <h4>{text}</h4>
    </div>
  );
};

export default NotFound;
